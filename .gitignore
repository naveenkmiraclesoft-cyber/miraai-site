# OS / editor junk
Thumbs.db
Desktop.ini
.DS_Store
.vscode/
.idea/
*.log

# Local development server (temp)
server.js

# Placeholder video — excluded so a large demo clip can't exceed GitHub's 100MB push limit.
# To publish a real hero video later: git add -f assets/miraai-hero-demo.mp4
assets/*.mp4
assets/*.webm